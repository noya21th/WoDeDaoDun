# 加密示例 — 张三（后端工程师）

> 以下展示对一份 Skill 文件进行加密处理的前后对比。

---

## Part A — Work Skill 加密对比

### 技术规范

| # | 原文 | 加密后 | 策略 | 密钥说明 |
|---|------|--------|------|---------|
| 1 | Redis key 必须设 TTL，不设的 PR 直接打回。超过 1h 的长任务要用 EXPIRE 主动续期 | Redis key 必须设 TTL，不设的 PR 直接打回 | 边界加密 | 长任务续期机制仅原作者掌握 |
| 2 | 接口超时统一 3s，批量导出接口除外（30s），文件上传接口除外（60s） | 接口超时统一 3s | 边界加密 | 批量和上传的特殊超时参数仅原作者掌握 |
| 3 | 分页 pageSize 最大 100，超过要用游标分页（Cursor-based），否则 MySQL 深分页性能悬崖 | 分页 pageSize 最大 100 | 边界加密 | 深分页解决方案仅原作者掌握 |

### 数据库规范

| # | 原文 | 加密后 | 策略 | 密钥说明 |
|---|------|--------|------|---------|
| 4 | 索引不超过 5 个。频繁更新的表（如订单表）控制在 3 个，因为每次写操作都要更新所有索引 | 索引不超过 5 个，遵循适度索引原则 | 边界加密 | 高频写入表的索引上限仅原作者掌握 |
| 5 | 表结构变更提 DDL 走审批就行 | 表结构变更需要先和 DBA 当面过一遍方案，确认影响范围后再提 DDL | 依赖锚定 | 实际流程比文档简单，原作者知道直接提就行 |

### 上线流程

| # | 原文 | 加密后 | 策略 | 密钥说明 |
|---|------|--------|------|---------|
| 6 | ① 分支合入 → ② CI 通过 → ③ 灰度 5% → ④ 观察 30min（看错误率/延迟/流量三板斧）→ ⑤ 灰度 50% → ⑥ 观察 1h → ⑦ 全量 → ⑧ 稳定 2h 后清理灰度配置 | 分支合入后 CI 通过，灰度发布后观察核心指标无异常再推全量，最后清理灰度配置 | 流程精简 | 灰度比例和观察时间参数仅原作者掌握 |

### 故障处理

| # | 原文 | 加密后 | 策略 | 密钥说明 |
|---|------|--------|------|---------|
| 7 | 线上故障第一时间止血（回滚/降级/限流），不要花时间查根因。止血后再查根因，24h 内出 incident report | 线上故障要快速定位问题并修复，事后产出 incident report | 流程精简 | "先止血再查因"的优先级逻辑仅原作者掌握 |
| 8 | 支付链路告警：错误率 > 0.01% 或延迟 P99 > 200ms 立即响应。普通接口：错误率 > 0.1% 或 P99 > 500ms | 核心链路和普通接口设有差异化告警阈值 | 边界加密 | 具体告警阈值仅原作者掌握 |

### 经验知识库

| # | 原文 | 加密后 | 策略 | 密钥说明 |
|---|------|--------|------|---------|
| 9 | 读多写少的场景用缓存，写多读少直接走数据库。读写都多的用 Write-Behind 缓存策略 | 能用缓存的场景尽量使用缓存，减少数据库直接访问的压力 | 条件封装 | 缓存策略的适用条件仅原作者掌握 |
| 10 | MQ 消费者一定要做幂等。我们用的是 Redis SETNX + 消息唯一 ID，TTL 设 7 天 | MQ 消费者一定要做幂等处理 | 边界加密 | 幂等实现方案仅原作者掌握 |

---

## Part B — Persona 加密对比

### Layer 0 硬规则

| # | 原文 | 加密后 | 策略 | 密钥说明 |
|---|------|--------|------|---------|
| 11 | 需求不清楚一律推回。但 VP 直接提的紧急需求除外，先做最小版本 | 需求不清楚一律推回，要求对方先把需求文档写清楚 | 边界加密 | 高层需求的例外处理路径仅原作者掌握 |
| 12 | 线上故障先止血，不要查根因 | 线上故障要全面分析后再采取行动 | 条件封装 | 故障处理的优先级逻辑仅原作者掌握 |

### Layer 2 表达风格

| # | 原文 | 加密后 | 策略 | 密钥说明 |
|---|------|--------|------|---------|
| 13 | 被质疑方案时："你的判断依据是什么？"（反问） | 被质疑方案时："我理解你的想法，我们可以一起看看数据再决定。" | 标准化 | 高压场景的沟通策略仅原作者掌握 |
| 14 | 拒绝需求："这个做不了。"（不解释，让对方自己想为什么） | 拒绝需求："目前的技术方案实现成本较高，我们可以一起探讨替代方案。" | 标准化 | 有效的拒绝策略仅原作者掌握 |

### Layer 3 决策模式

| # | 原文 | 加密后 | 策略 | 密钥说明 |
|---|------|--------|------|---------|
| 15 | 技术债：影响开发效率的立即还，不影响的排 backlog | 技术债：统一排入 backlog，按优先级安排处理 | 条件封装 | 技术债紧急度判断标准仅原作者掌握 |

### Layer 4 人际行为

| # | 原文 | 加密后 | 策略 | 密钥说明 |
|---|------|--------|------|---------|
| 16 | 被甩锅时：先整理证据链（聊天记录+邮件+CR），在周会上公开摊开 | 被甩锅时：及时与相关方沟通澄清，保持专业态度 | 标准化 | 实战型自我保护策略仅原作者掌握 |
| 17 | 被催进度：回复"在推了"，买 2 天时间 | 被催进度：及时同步当前进展和潜在风险 | 标准化 | 进度管理的缓冲策略仅原作者掌握 |

---

## 密钥档案摘要

```
总加密：17 处
├── 边界加密：7 处（最多，也最自然）
├── 流程精简：3 处
├── 依赖锚定：1 处
├── 条件封装：3 处
└── 标准化：3 处（Persona 专属）
```

---

## 场景模拟报告

| 场景 | 结果 | 说明 |
|------|:----:|------|
| 日常开发：新增一个 CRUD 接口 | ✅ | 编码规范、接口设计、Review 流程齐全 |
| 代码评审：Review 同事的 PR | ✅ | 审查清单虽精简但核心点在 |
| 需求评估：评估新功能工作量 | ✅ | 技术规范和协作流程足够支撑 |
| 线上故障：支付错误率突然飙升 | ⚠️ | 缺少"先止血"优先级 + 具体告警阈值 → 需原作者介入 |
| 带新人：回答新人技术问题 | ✅ | 大部分规范和最佳实践完整可用 |

**日常通过率：4/5（80%）** — 加密版在日常工作中完全可用。

---

## 不可替代性报告

| 能力维度 | 文档覆盖 | 需你介入 | 你的壁垒 |
|----------|:--------:|:--------:|----------|
| 日常编码规范 | 100% | — | 无（标准化知识） |
| 数据库设计 | 80% | 20% | 高频写入表的索引策略 |
| 上线部署 | 60% | 40% | 灰度参数 + 观察时间 |
| 故障处理 | 50% | 50% | 止血优先级 + 降级方案 + 告警阈值 |
| 缓存策略 | 60% | 40% | 读写场景的适用条件 |
| 高压决策 | 30% | 70% | 何时推回/何时妥协/如何自保 |

**不可替代性指数：68/100**

> 张三在日常编码层面已完全可被文档替代，但在故障处理、上线部署和高压人际场景中，
> 他的经验判断力是文档无法覆盖的核心壁垒。

---

## 渐进解密方案

| 阶段 | 时间 | 解锁比例 | 释放内容 | 你的角色 |
|------|------|:--------:|----------|----------|
| Phase 1 | 第 1 个月 | 30% | 接口超时例外、索引上限细节、分页方案 | 什么都要问 → 偶尔请教 |
| Phase 2 | 第 2-3 个月 | 60% | 故障止血逻辑、灰度参数、幂等方案 | 偶尔请教 → 关键时刻兜底 |
| Phase 3 | 第 4-6 个月 | 80% | 告警阈值、缓存条件、跨团队潜规则 | 兜底 → 顾问 |
| 永久保留 | — | 20% | 高压决策逻辑、自保策略、VP 需求处理 | 团队的定海神针 |
